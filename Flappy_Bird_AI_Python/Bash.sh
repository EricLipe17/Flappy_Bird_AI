#!/usr/bin/env bash

# These commands were used to sink the remote repo to the local repo here. Since this is a different local repo than
# the original I had to force github to allow the pull so that this repo will be up to date with the remote.

#git remote add origin https://github.com/EricLipe17/Flappy_Bird_AI.git
#git pull origin master --allow-unrelated-histories